package com.example.mobdeve.s13.payao.malcolm.fitme.database

import com.example.mobdeve.s13.payao.malcolm.fitme.models.Exercise
/*

class ExerciseDataHelper {
    companion object {
        fun initializeExercises(): ArrayList<Exercise> {
            val exercises = ArrayList<Exercise>()

            exercises.add(
                Exercise(
                    1,
                    "Push-up",
                    reps = 10,
                    sets = 1,
                    KG= 0,
                    instruction = "Place your hands shoulder-width apart on the floor. Keep your body in a straight line from head to heels. Lower your body until your chest nearly touches the floor. Push back up."
                )
            )

            exercises.add(
                Exercise(
                    2,
                    "Pull-up",
                    reps = 8,
                    sets = 2,
                    KG = 10,
                     instruction = "Grab the pull-up bar with an overhand grip. Hang at arm's length. Pull your chest to the bar by bending your elbows and squeezing your shoulder blades together. Lower yourself back to the starting position."
                )
            )

            exercises.add(
                Exercise(
                    3,
                    "Squats",
                    reps = 12,
                    sets = 3,
                    KG = 5,
                    instruction = "Stand with your feet shoulder-width apart. Lower your body as if you're sitting back into a chair. Keep your chest up and your knees behind your toes. Push through your heels to return to the starting position."
                )
            )

            exercises.add(
                Exercise(
                    4,
                    "Planks",
                    reps = 15,
                    sets = 4,
                    KG = 0,
                    instruction = "Start in a push-up position, with your forearms on the ground. Keep your body in a straight line from head to heels. Hold this position, engaging your core muscles, for the prescribed time."
                )
            )

            return exercises
        }


        fun getMaxKG(exercises: List<Exercise>): Int {
            var maxKG = Int.MIN_VALUE
            for (exercise in exercises) {
                if (exercise.KG > maxKG) {
                    maxKG = exercise.KG
                }
            }
            return maxKG
        }

        fun getMaxReps(exercises: List<Exercise>): Int {
            var maxReps = Int.MIN_VALUE
            for (exercise in exercises) {
                if (exercise.reps > maxReps) {
                    maxReps = exercise.reps
                }
            }
            return maxReps
        }

        fun getMaxSession(exercises: List<Exercise>): Int {
            var maxSession = Int.MIN_VALUE
            for (exercise in exercises) {
                if (exercise.exerciseId > maxSession) {
                    maxSession = exercise.exerciseId
                }
            }
            return maxSession
        }
    }


}
*/

