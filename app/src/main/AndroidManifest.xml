<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.mobdeve.s13.payao.malcolm.fitme"
    >

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FitMe"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".models.Stats"/>

        <activity android:name=".models.Instructions" />
        <activity android:name=".activities.AddNewExerciseActivity" />


        <activity android:name=".activities.RegisterActivity"/>
        <activity android:name=".activities.ForgotActivity"/>
        <activity android:name=".activities.Login"/>
        <activity android:name=".models.Settings" />
        <activity android:name=".models.EditProfile" />
        <activity android:name=".models.Account" />
        <activity android:name=".models.ChangeAccountSettings" />

        <activity android:name=".activities.CreateNewWorkout">
        </activity>

        <activity android:name=".activities.ViewWorkout">
        </activity>
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>